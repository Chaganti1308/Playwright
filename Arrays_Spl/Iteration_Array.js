let num_array = [10,20,30,40,50,60];

// using index as well
num_array.forEach((a,i) => {
    console.log(`${a} : ${i}`);
    
});

num_array.forEach(num => {
    console.log("Array Element : ",num);
    
})